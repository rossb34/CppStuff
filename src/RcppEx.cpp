/*
 * RcppEx.cpp
 *
 *  Created on: Dec 5, 2012
 *      Author: rossbennett
 */

/*
#include <iostream>
#include <vector>
#include <numeric>

template <typename T>
T sum(std::vector<T> &a){
        T result = 0;
        for(int ii = 0; ii < a.size(); ii++){
                result += a[ii];
        }
        return result;
}

template <typename T>
T sum3(std::vector<T> &a){
        T result = std::accumulate(a.begin(), a.end(), 0.0);
        return result;
}

int main() {
        std::vector<double> vec(5, 3.3);

        int num1 = sum3(vec);

        // int num2 = sum2(vec);

        // int num1 = std::accumulate(vec.begin(), vec.end(), 0);

        // int num3 = sum(vec);

        for(int ii = 0; ii < vec.size(); ii++){
                std::cout << vec[ii] << " ";
        }

        std::cout << std::endl;
        std::cout << "the sum3 of the vector is: " << num1 << std::endl;
        // std::cout << "the sum2 of the vector is: " << num2 << std::endl;
        // std::cout << "the template sum of the vector is: " << num3 << std::endl;


        for(int ii = 0; ii < vec.size(); ii++){
                std::cout << vec[ii] << " ";
        }
        return 0;
}
 */
